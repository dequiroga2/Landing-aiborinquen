// server/db.ts
// DB disabled: this project runs webhook-only for now.
// If you later re-enable DB, restore the Drizzle/Postgres implementation.

export const db = null as any;
export const pool = null as any;
